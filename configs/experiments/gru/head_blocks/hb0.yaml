# head_num_blocks=0 (effectively no head)
num_training_batches: 20000

model:
  head_type: "resffn"
  head_norm: "layernorm"
  head_dropout: 0.1
  head_activation: "gelu"
  head_num_blocks: 0

wandb:
  group: head_blocks
  run_name: hb0
  tags: ["head_sweep", "resffn", "layernorm", "hb0"]
