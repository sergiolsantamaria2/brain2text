model:
  decoder_type: gru

lr_scheduler_type: cosine
lr_max: 0.002
lr_min: 0.0001
lr_warmup_steps: 1000

lr_max_day: 0.001
lr_min_day: 0.0001
lr_warmup_steps_day: 1000

output_dir: trained_models/lr_warmup/gru_w1000_lr2e-3_5k_${now:%Y-%m-%d_%H%M%S}
checkpoint_dir: ${output_dir}/checkpoint

wandb:
  run_name: gru_w1000_lr2e-3_5k_${now:%Y-%m-%d_%H%M%S}
  tags: ["lr", "warmup", "cosine", "gru", "5k", "w1000", "lr2e-3"]