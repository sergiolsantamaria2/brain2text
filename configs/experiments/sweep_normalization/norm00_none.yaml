output_dir: ${oc.env:SLURM_TMPDIR,/tmp}/brain2text/trained_models/sweep_normalization_10k/norm00_none
checkpoint_dir: ${output_dir}/checkpoint

num_training_batches: 10000
seed: 10
dataset:
  seed: 10

wandb:
  enabled: true
  project: brain2text
  group: sweep_normalization_10k
  run_name: norm00_none_seed10_10k
  tags: ["norm_sweep", "none", "seed10", "10k"]

model:
  head_type: "none"

dataset:
  data_transforms:
    feature_norm: "none"
