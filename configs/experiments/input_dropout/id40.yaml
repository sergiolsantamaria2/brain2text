# input_layer_dropout=0.4
num_training_batches: 20000
lr_decay_steps: ${num_training_batches}
lr_decay_steps_day: ${num_training_batches}

model:
  input_network:
    input_layer_dropout: 0.4

wandb:
  group: input_dropout
  run_name: id40
  tags: ["input_dropout", "input_layer_dropout_0.4"]
