# days_per_batch=12
num_training_batches: 20000
lr_decay_steps: ${num_training_batches}
lr_decay_steps_day: ${num_training_batches}

dataset:
  days_per_batch: 12

wandb:
  group: days_per_batch
  run_name: dpb12
  tags: ["days_per_batch", "dpb12"]
