output_dir: trained_models/sweep_step1/w500_lr2e-3_day1e-3_20k_${now:%Y-%m-%d_%H%M%S}
checkpoint_dir: ${output_dir}/checkpoint
num_training_batches: 20000

lr_scheduler_type: cosine
lr_max: 0.002
lr_min: 0.0001
lr_warmup_steps: 500
lr_decay_steps: 20000

lr_max_day: 0.001
lr_min_day: 0.0001
lr_warmup_steps_day: 500
lr_decay_steps_day: 20000

save_all_val_steps: false
save_val_logits: false

wandb:
  enabled: true
  project: brain2text
  run_name: sweep_w500_lr2e-3_day1e-3_20k_${now:%Y-%m-%d_%H%M%S}
  tags: ["sweep", "step1", "cosine", "warmup500"]
