output_dir: trained_models/reslstm/sanity_ln_pre
checkpoint_dir: ${output_dir}/checkpoint

num_training_batches: 200
batches_per_val_step: 200
batches_per_train_log: 20

save_all_val_steps: false
save_val_logits: false
save_val_data: false

model:
  decoder_type: reslstm
  reslstm_norm: layernorm
  reslstm_post_norm: false

wandb:
  enabled: true
  project: brain2text
  run_name: reslstm_sanity_ln_pre
  tags: ["reslstm", "sanity", "layernorm", "pre"]
