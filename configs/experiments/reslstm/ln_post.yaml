output_dir: trained_models/reslstm/ln_post
checkpoint_dir: ${output_dir}/checkpoint

model:
  decoder_type: reslstm
  reslstm_norm: layernorm
  reslstm_post_norm: true

wandb:
  enabled: true
  project: brain2text
  run_name: reslstm_ln_post
  tags: ["reslstm", "layernorm", "post"]
