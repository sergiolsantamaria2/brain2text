output_dir: trained_models/gru_lr0p002_day0p001_longrun
checkpoint_dir: trained_models/gru_lr0p002_day0p001_longrun/checkpoint
num_training_batches: 120000

lr_scheduler_type: cosine
lr_max: 0.002
lr_min: 0.0001
lr_warmup_steps: 1000
lr_decay_steps: 120000

lr_max_day: 0.001
lr_min_day: 0.0001
lr_warmup_steps_day: 1000
lr_decay_steps_day: 120000

wandb:
  enabled: true
  project: brain2text
  run_name: gru_lr_warmup_cosine
  tags: ["lr", "warmup", "cosine"]
