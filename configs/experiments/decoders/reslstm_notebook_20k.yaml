model:
  decoder_type: reslstm
  reslstm_lstm_layers: 2
  reslstm_lstm_dropout: 0.1

gpu_number: "0"

output_dir: trained_models/compare_decoders/reslstm_20k_ln_pre_${now:%Y-%m-%d_%H%M%S}
checkpoint_dir: ${output_dir}/checkpoint

num_training_batches: 20000

dataset:
  num_dataloader_workers: 1

save_all_val_steps: false
save_val_logits: false

wandb:
  enabled: true
  project: brain2text
  run_name: compare_reslstm_notebook_20k
  tags: ["compare", "reslstm", "notebook", "20k"]
