model:
  decoder_type: xlstm
  xlstm_num_blocks: 5
  xlstm_num_heads: 4
  xlstm_conv1d_kernel_size: 4
  xlstm_dropout: 0.2

gpu_number: "0"

output_dir: trained_models/compare_decoders/xlstm_20k_${now:%Y-%m-%d_%H%M%S}
checkpoint_dir: ${output_dir}/checkpoint

num_training_batches: 20000

dataset:
  num_dataloader_workers: 1

save_all_val_steps: false
save_val_logits: false

wandb:
  enabled: true
  project: brain2text
  run_name: xlstm_20k
  tags: ["compare", "xlstm", "20k"]
